# Rule: Command Execution Preferences

- **Do NOT automatically execute** `npm run build`, `npm run deploy` / `npm deploy`, or `npm run dev`.
- Only run these commands when the user explicitly requests them.
- Do **NOT** ask or suggest at the end of messages/prompts if the user wants to run these commands.
